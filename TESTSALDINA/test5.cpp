#include <iostream>

/*POINTERS - RETURN MULTIPLE VALUES.
void minMax(int number[], int size, int *min, int *max) {
    for (int i = 1; i < size; ++i) {
        if (*(number + i) < *min) {
            *min = *(number + i);
        }
        if (*(number + i) > *max) {
            *max = *(number + i);
        }
    }
}

int main() {
    const int size = 5;
    int number[] {2, -8, -1, 0, 4, -3};

    int max = *number;
    int min = *number;
    minMax(number, size, &min, &max);

    std::cout << "Max number is: " << max << std::endl;
    std::cout << "Min number is: " << min << std::endl;

    std::cin.get();
    return 0;
}
*/

int main() {

    std::cin.get();
    return 0;
}